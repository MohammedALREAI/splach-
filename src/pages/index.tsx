import React from "react";
import { Meta } from "../Hoc/Meta";
import { Meta } from "../Hoc/Meta";

interface Props {
  page?: string;
}

export const index = (props: Props) => {
  return <div>

    {/* <Meta title={"HOME PAGE"} /> */}
  </div>;
};
